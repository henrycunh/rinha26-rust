[package]
name = "rinha-fraud"
version = "0.1.0"
edition = "2021"

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = 3
overflow-checks = false
panic = "abort"
strip = true

[dependencies]
libc = "0.2"
